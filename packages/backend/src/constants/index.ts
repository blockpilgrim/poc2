/**
 * Application Constants
 * Central export for all application constants
 */

// Note: d365 and lead constants moved to _unused_implementations
// The active implementation uses d365-mappings.ts directly